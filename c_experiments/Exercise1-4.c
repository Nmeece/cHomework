#include <stdio.h>

int main (void)
{
    int num1, num2, diff;

    num1 = 15;
    num2 = 87;
    diff = num2 - num1;

    printf("The difference between %i and %i is %i.\n", num2, num1, diff);

    return(0);
}